# GuardianLens Backend Dependencies
# Render-compatible: no system binaries, no C compiler needed

# Core Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
alembic==1.13.3

# Config
pydantic-settings==2.4.0
python-dotenv==1.0.1

# File Handling
python-multipart==0.0.12
aiofiles==23.2.1

# Image Processing (ELA core)
Pillow==10.4.0
numpy==1.26.4

# Google Gemini AI
# google-genai 1.9.0 requires httpx>=0.28.1,<1.0.0
google-genai==1.9.0
httpx>=0.28.1,<1.0.0

# PDF Support (pure-python only)
PyPDF2==3.0.1
# pdf2image needs poppler binary — Docker only, skip on Render
# pytesseract needs Tesseract binary — Docker only, skip on Render

# Certificate Generation
reportlab==4.2.5
qrcode[pil]==8.0

# Rate Limiting
slowapi==0.1.9

# Logging
structlog==24.4.0
